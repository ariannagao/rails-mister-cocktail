<div class="index-banner col-12">

<div class="container">
    <ul class= "index-list">
    <% @cocktails.each do |cocktail| %>
    <li><%= link_to cocktail.name, cocktail_path(cocktail)%></li>
    <% end %>
  </ul>
  <%= link_to "+", new_cocktail_path, class: "btn-brown" %>
</div>
</div>
